- defaults:
    link:
      relink: true
    shell:
      stdout: true
      stderr: true

- clean:
    ~/:
      force: true
    ~/.config:
      recursive: true

- create:
    - ~/.dotbot-git

- brewfile:
    - Brewfile

# Git 
- link:
    ~/.gitconfig:
      path: .gitconfig
      force: true
      create: true

# SSH
# Runs after the Brewfile has installed Secretive. The agent socket still will
# not exist until Secretive.app has been launched and a key created, which is
# why ssh/config guards the IdentityAgent line on the socket being there.
# force: true replaces an existing ~/.ssh/config -- check before re-running on
# a machine that already has one.
- shell:
    - [mkdir -p ~/.ssh && chmod 700 ~/.ssh, Ensure ~/.ssh exists with safe permissions]
- link:
    ~/.ssh/config:
      path: ssh/config
      force: true
      create: true

# Powerline fonts
- git:
    ~/.dotbot-git/fonts:
        url: https://github.com/powerline/fonts.git
        description: Powerline fonts

- shell:
  - [cd ~/.dotbot-git/fonts && ./install.sh, "Install Powerline fonts"]

# Install Zsh and Oh My Zsh
- shell:
  - ['[ ! -f ~/.zshrc ] || cp ~/.zshrc ~/.zshrc.bak', Backup existing .zshrc if there is one]
- link:
    ~/.zshrc:
      path: zsh/.zshrc.oh-my-zsh
      force: true
    ~/.zshrc.custom:
      path: zsh/.zshrc.custom
      force: true

## Oh My Zsh
- shell:
  - [rm -rf ~/.oh-my-zsh, Remove Oh My Zsh]
- git:
    ~/.oh-my-zsh:
        url: https://github.com/ohmyzsh/ohmyzsh.git
        description: Oh My Zsh
- git:
    ~/.config/oh-my-zsh/custom/plugins/zsh-syntax-highlighting:
        url: https://github.com/zsh-users/zsh-syntax-highlighting.git
    ~/.config/oh-my-zsh/custom/plugins/zsh-autosuggestions:
        url: https://github.com/zsh-users/zsh-autosuggestions.git
- link:
    ~/.config/oh-my-zsh/custom/aliases.zsh:
      path: zsh/custom/aliases.zsh
      create: true
      force: true


# Karabiner elements config
# Karabiner doesn't like direct linking of config file, instead we should symlink a directory
- link:
    ~/.config/karabiner:
      path: karabiner
      force: true
      create: true

# OS X
# NOT under sudo: `defaults write` targets the effective user's preference
# domain, so running this as root wrote every setting into /var/root and the
# logged-in account never saw them. The script elevates the two commands that
# genuinely need root (pmset) on its own.
# stdin is required so sudo/sysadminctl password prompts can reach the terminal.
- shell:
    - command: macos/set-defaults.sh
      description: Setting up macOS defaults
      stdin: true
      stdout: true
      stderr: true

# Commit signing
# Provisions ~/.ssh/git-signing.pub from the agent and checks that SecretAgent
# can post notifications. Best-effort and advisory: Secretive keys cannot exist
# before the app has been launched and keys created, so on a first run this
# prints what is still missing rather than failing. Runs after set-defaults.sh
# so its warnings land near the other security warnings.
- shell:
    - command: macos/check-signing.sh
      description: Checking commit signing
      stdout: true
      stderr: true

# Follow ups
- shell:
    - command: |
        echo ""
        echo "  Commit signing is ON, so 'git commit' fails until"
        echo "  ~/.ssh/git-signing.pub exists. Two keys are needed."
        echo ""
        echo "  In Secretive (allow notifications when asked):"
        echo "    1. create a key, 'Require Authentication'. This is the"
        echo "       SSH auth key. Add it to GitHub as an AUTHENTICATION key."
        echo "    2. create a second key named exactly 'git-signing', with"
        echo "       'Notify' instead of 'Require Authentication'. Add it to"
        echo "       GitHub as a SIGNING key -- a list separate from"
        echo "       Authentication keys, and a key in only the latter signs"
        echo "       fine but never shows a Verified badge."
        echo ""
        echo "  Two keys because signing hits the Secure Enclave once per"
        echo "  commit, and ControlMaster cannot amortise that the way it"
        echo "  does for pushes. One auth-required key means a Touch ID tap"
        echo "  on every single commit."
        echo ""
        echo "  Then, in a new shell:"
        echo "    ssh-add -L | grep git-signing > ~/.ssh/git-signing.pub"
        echo "    ssh -T git@github.com"
        echo ""
      description: Next steps
      stdout: true
    - [echo "You're done!"]
